# Fire Engine — File Review Order

A complete, file-by-file reading order for understanding the entire system, arranged so each
tier depends only on the tiers above it. Read headers before their `.cpp`; within a tier, go
top-to-bottom.

This complements [`onboarding.md`](onboarding.md) (architecture + recommended route) and [`collision.md`](collision.md) (physics
authoring). Where the two overlap, [`onboarding.md`](onboarding.md) carries the prose; this file is the exhaustive
ordered checklist with per-file attention points.

---

## Build and tooling files

Read these first when a change touches build configuration, CI, or local tooling:

| File | Pay attention to |
|---|---|
| `CMakePresets.json` | The `vcpkg` preset pins Apple Clang on macOS, selects `Dev`, and exports `compile_commands.json` for `clangd`. |
| `CMakeLists.txt` | `Dev` is the default build type (`-O2 -g`, no `NDEBUG`); `FIRE_ENGINE_WARNINGS_AS_ERRORS` is CI-only by default; CTest registers `test_fire_engine` (`~[slow]`) so plain CTest stays fast; `tests-full` runs the all-tags Catch2 binary plus the layering guard; `run-clang-tidy` appears only when `clang-tidy` is installed. |
| `.clang-format` / `.clang-tidy` | Formatting is CI-gated. Tidy is the first-pass static-analysis config for engine `src/` plus `include/fire_engine/`; disabled checks are documented inline. |
| `.github/workflows/ci.yml` | Builds with vcpkg + warnings-as-errors, runs `run-clang-tidy`, runs the full `tests-full` target, and has a separate clang-format dry-run job. |

## Tier 0 — Math & value types (foundation, no dependencies)

| File | Pay attention to |
|---|---|
| `math/constants.hpp` | Just π/epsilon constants — orient quickly. |
| `math/vec_base.hpp` | CRTP base for the vec types; compound-assign are primitives, binary ops delegate. |
| `math/vec2.hpp` / `vec3.hpp` / `vec4.hpp` | `Vec3` is the workhorse. **`magnitude()`/`normalise()` are deliberately NOT constexpr** (sqrt). `operator==` is **strict bit equality** — use `approxEqual` for tolerance. Vec3↔Vec4 conversions are `explicit` both ways. |
| `math/quaternion.hpp` | SLERP, `fromVectors`, Hamilton `operator*`, and `integrate(ω, dt)` (exponential-map orientation integration for the rigid-body solver). Used for all scene rotation; glTF round-trips through this. |
| `math/mat3.hpp` | Column-major 3×3 (mirrors `Mat4`'s `[row,col]`): `fromQuaternion`, `diagonal`, `transpose`, `Mat3·Mat3` / `Mat3·Vec3`. Holds the world inverse inertia `R·diag(invI)·Rᵀ` in the physics solver. |
| `math/mat4.hpp` | **Column-major.** Translation/rotation/scale/perspective/look-at. Everything downstream trusts this — verify the multiplication and handedness conventions. |
| `math/view_basis.hpp` | Shared right/up construction that stays finite for zero-length or vertical look dirs. Used by view, skybox, shadow-fit, sort-depth. |
| `graphics/colour3.hpp` | Trivial RGB value type. |

## Tier 1 — Handles, limits, tunables (vocabulary types)

| File | Pay attention to |
|---|---|
| `graphics/gpu_handle.hpp` | Opaque handle types — the contract that keeps `graphics/` Vulkan-free. |
| `graphics/gpu_limits.hpp` | `kMaxLights`, `kMaxJoints`, `kMaxMorphTargets`, shadow caster caps, `kShadowTotalMatrixCount`. **These must equal shader array sizes** — cross-file invariant. |
| `render/constants.hpp` | Scalar render tunables (biases, IBL strengths, extents, FOV). Includes gpu_limits. Note the bias values — comments explain why they're conservative. |
| `physics/physics_handle.hpp` / `collision/collider_id.hpp` | Stable opaque IDs linking scene↔physics↔broadphase. |
| `core/log.hpp` | Runtime diagnostics. `FE_LOG` is parsed once; levels are `debug`/`info`/`warn`/`error`/`off`, with category overrides like `ragdoll:debug`. Engine code should use `log::debug/info/warn/error`, not direct stream or printf diagnostics. |

## Tier 2 — Input (pre-packaged before scene)

| File | Pay attention to |
|---|---|
| `input/camera_state.hpp`, `controller_state.hpp`, `animation_state.hpp`, `variant_state.hpp` | Small per-domain state bundles. |
| `input/input_state.hpp` | The per-frame bundle handed to `SceneGraph::update`. |
| `input/input.hpp` + `src/input/input.cpp` | GLFW-facing collector that fills `InputState`. **Key point: components never poll GLFW — they read normalized `InputState`.** |

## Tier 3 — Platform & process (thin OS wrappers)

| File | Pay attention to |
|---|---|
| `core/system.hpp` + `system.cpp` | GLFW init/destroy, time. |
| `platform/keyboard.hpp` + `keyboard.cpp` | **`enum class Key` order must match `kGlfwKeyCodes` table** — a reorder silently maps wrong keys (count mismatch fails compile, reorder doesn't). |
| `platform/mouse.hpp` + `mouse.cpp`, `window.hpp` + `window.cpp` | GLFW window/surface lifetime, resize callback. |
| `platform/application_args.hpp` + `src/platform/application.cpp` | CLI parsing → `RendererDebug{ DebugView view; bool noShadows; }`. This is the entry `main`. |

## Tier 4 — Scene graph (runtime behaviour, easiest to reason about)

| File | Pay attention to |
|---|---|
| `scene/transform.hpp` + `transform.cpp` | Local TRS + cached local/world matrices. Cache invalidation is the thing to watch. |
| `scene/components.hpp` | `std::variant<Empty,Animator,Camera,Mesh,Light,ParticleEmitter>`. Access via `componentAs<T>()`/`visitComponent` — not raw `std::get_if`. |
| `scene/empty.hpp` + `empty.cpp`, `controllable.hpp` + `controllable.cpp` | Empty = structural no-op/joints. Controllable = optional gameplay movement, **separate from the variant**. |
| `scene/camera.hpp` + `camera.cpp` | Consumes `CameraState`; exposes world position/target for `drawFrame`. |
| `scene/light.hpp` + `light.cpp` | Point/spot/directional runtime light. |
| `scene/particle_emitter.hpp` + `particle_emitter.cpp` | GPU particle emitter component. Transform-driven; gathered (not drawn) like `Light` via `toEmitterState` → `gatherEmitters`. The GPU pool/sim lives in `render/particle_system`. |
| `scene/mesh.hpp` + `mesh.cpp` | Wraps graphics `Object`; updates morph weights; emits draw commands in render traversal. |
| `scene/node.hpp` + `node.cpp` | Owns transform, component, optional controllable, optional physics handles, children, parent, cached `composedWorld`. The `requires`-based no-op render is here. `render` takes a Vulkan-free `SceneDrawContext` and skips draw-building for nodes in `SceneDrawContext::culledNodes` (still recurses children — Mesh returns `world`, so it's equivalent). |
| `scene/node_format.hpp` / `scene_graph_format.hpp` | Formatters split out so `<format>` doesn't bleed into hot headers — include only at print sites. |
| `scene/scene_graph.hpp` + `scene_graph.cpp` | **Core runtime loop**: `update` → `submitPhysics` → `applyPhysics`/`resolve`, `gatherLights`/`gatherEmitters`, render traversal. Study the physics submit/apply ordering and the Static/Kinematic/Dynamic authority model. Owns the `SceneCuller`; `cull(frustums)` runs the coarse pre-cull before `render`. Also owns the **active camera** (`activeCamera(Node*)` setter + `activeCamera()` seam override → live world pose, or a fixed debug fallback when unset). |
| `scene/scene_culler.hpp` + `scene_culler.cpp` | Persistent `AabbBvh<Node*>` over rigid renderable nodes. `sync()` reconciles the proxy set and re-transforms a proxy's world bound only when the node's `worldRevision()` changed (a per-`Node` counter bumped on world-matrix change), so a static scene does no per-frame bound recompute; `cull(frustums)` unions the leaves visible in *any* frustum (camera + every shadow caster) and returns the nodes in **none** of them — the set `Node::render` skips. Deformable (skinned/morph) nodes are **never tracked** (always drawn, left to the precise per-bucket cull). |

## Tier 5 — Animation (data-first, renderer-independent)

| File | Pay attention to |
|---|---|
| `animation/animation_selection.hpp` | Clip selection for switching. |
| `animation/animation.hpp` + `src/animation/animation.cpp` | LINEAR/STEP/CUBICSPLINE sampling; rotation via SLERP. **Morph weights sampled separately from the TRS matrix** — mesh can animate vertex deltas without moving the node. |
| `scene/animator.hpp` + `animator.cpp` | Scene component that samples `Animation` and pushes the sampled matrix down the tree. |

## Tier 6 — Graphics data (still Vulkan-free)

| File | Pay attention to |
|---|---|
| `graphics/vertex.hpp`, `joints4.hpp` | Vertex layout (pos/colour/normal/tangent/2×UV/joints/weights); fixed 4-joint skinning influence. |
| `graphics/bounds.hpp`, `sampler_settings.hpp` | Value types; sampler filter/wrap from glTF. `Bounds3` is the growable world-space AABB used for shadow fit + frustum culling. |
| `graphics/frustum.hpp` | Vulkan-free, header-only. 6 Gribb–Hartmann planes from a viewProj (**Vulkan [0,1] depth** — near plane = row 2, not row3+row2) + a positive-vertex `intersects(Bounds3)` test (**conservative — no false negatives**; invalid bounds always visible). Tested in `test_frustum.cpp`. |
| `graphics/image.hpp` + `image.cpp` | **Reference class for the whole code style.** Read it as the style exemplar. |
| `graphics/ktx_image.hpp` + `ktx_image.cpp`, `texture.hpp` + `texture.cpp` | KTX2/Basis CPU container; `Texture` holds a `TextureHandle` after upload. |
| `graphics/material.hpp` + `material_binding.hpp`/`.cpp` | Ten textures in one array via `material.texture(MaterialTextureSlot::X)` → `TextureSlot{texture,texCoord,transform,has()}`. Clearcoat/transmission(+ior)/volume are `std::optional<…Params>`. `toMaterialUBO` packs into `MaterialUBO` (`value_or({})` for absent blocks), including each slot's **bindless `textureIndex`** (= the texture's handle). **Slot enum order ↔ `MaterialUBO::uv[]`/`textureIndex[]` ↔ shader `SLOT_*` must move together** (no longer tied to descriptor bindings — textures are bindless). |
| `graphics/geometry.hpp` + `geometry.cpp` | Vertices/indices/material ptr/GPU handles/morph deltas. Note the `castsShadow` flag + `storageVertices` (cloth's compute-writable vertex buffer). `load()` also builds discrete **LODs** for static meshes >`kMinLodTriangles` (`std::vector<GeometryLod>`, all indexing the same vertex buffer). |
| `graphics/mesh_topology.hpp` + `mesh_topology.cpp` | **Shared LOD topology primitives** — `weldByPosition` (bit-exact position → canonical vertex), `wedgeDistance` (R⁵ attribute distance), `nearestWedge` (nearest wedge; **exact-distance ties break to the first = lowest stored index**), `canonicalWedges` (per-position wedge groups, vector-of-vectors), `canonicalWedgesCsr` (**the same buckets in a flat CSR** — `wedges` + `offsets`, `forCanonical(c) → span` — the GPU uploader's shape; per-bucket order is byte-for-byte the same as `canonicalWedges`, so both emit identical indices), `canonicalFaces`. One module consumed identically by the simplifier, VIPM and VDPM so the canonical-id contract can't drift between them (the seam where the recent LOD bugs clustered). Read first — the three below all build on it. `test_mesh_topology.cpp` pins CSR==vector-of-vectors bucket-for-bucket and the tie-break. |
| `graphics/lod.hpp`, `mesh_simplifier.hpp` + `mesh_simplifier.cpp` | **Mesh LOD foundation (spine #3 — full discrete→VIPM→VDPM ladder; authority is [`lod.md`](lod.md)).** `lod.hpp`: `GeometryLod{indexBuffer,indexCount,error}`, ratios/budgets, the `kVdpm*` dials, and the headless-testable `selectLod`. `mesh_simplifier`: a from-scratch **Garland–Heckbert QEM** with an **attribute-aware R⁵ (position + weighted-UV) quadric** so seams collapse *last*; **position-welded** connectivity (via `mesh_topology`) + a **wedge-preserving emit** (nearest-wedge); a **chart veto** (`canonicalCharts_`) that forbids a collapse crossing a UV/normal seam; and the **four VDPM deviation channels** recorded per collapse (`deviationRadius` point-to-plane, `uvDeviationRadius` max-accumulated per-wedge, `normal`/`tangentDeviationRadius` angular) plus a **conservative normal cone** (`normalConeAxis`/`normalConeCos`, accumulated bottom-up like `supportRadius` via the file-local `mergeCones` — conservative by construction: double math with re-normalised axes, axis re-checked against both children, one-ULP outward round; `normalConeCos <= 0` = "wider than a hemisphere ⇒ never cull" sentinel) for VDPM's per-split back-face/silhouette test (a bounding cap, not an exact oracle). `collapse()` also records each split's **`vl`/`vr` apexes** (`kNoCollapseApex` on a non-manifold welded edge) so the VDPM forest transcribes ground truth. Subset (endpoint) placement → LODs share the base vertex buffer. Records the ordered collapse stream (raw material for VIPM/VDPM). Headless `test_mesh_simplifier.cpp`. Two dials in the `.cpp`: `kUvWeightFactor`, `kErrorCeilingFactor`. |
| `graphics/vdpm_material.hpp` + `vdpm_material.cpp` | **Material-aware VDPM tolerances.** `vdpmChannelScales(material)` — a pure, Vulkan-free helper deriving the per-channel refine scales (`uv/normal/tangent`) for a material at refine time: 0 disables a channel the material can't show (unlit → normal+tangent; no tangent-space normal/clearcoat map → tangent; no textures → UV) and gloss scales the normal channel up (`kVdpmGlossyNormalBoost`). Called at the `object.cpp` VDPM refine site with `binding.activeMaterial`, feeding `refineForView`'s existing scale args — the simplifier/forest/front never see the material, so the collapse stream stays material-agnostic. Headless `test_vdpm_material.cpp`. |
| `graphics/vipm.hpp` + `vipm.cpp`, `graphics/vdpm.hpp` + `vdpm.cpp` | **Continuous LOD (Phases 2–3).** `vipm`: per-original-vertex geomorph payload + `selectVipm` (morphs full render attributes into the exact next LOD's wedge before the index swap; shader gates by target *level*, not error). `vdpm`: the **view-dependent** path — `VertexForest`/`buildVertexForest` (a straight **transcription** of the stream's recorded `vl`/`vr` apexes — no adjacency re-derivation; a non-manifold collapse leaves `removed` a root) and `ActiveFront` — `refineForView` (a **persistent** front — score pass → coarse-first refine over budget → fine-first coarsen under `kVdpmCoarsenRatio × budget`, the dead band being the anti-pop **hysteresis** — by the four screen-space channels + the **cone-driven** silhouette boost + back-face gate — `detail::coneVisibility`, an *exact evaluation of a conservative bound*: object-space (facing sign invariant under any linear transform), trig-free, reflection-exact via `sign(det)`, singular-safe, and gated by `rasterBackfaceCulling` from the material's cull mode; it replaced the old smooth-normal 4-witness proxy and ~halved `refineForView`), the **joint** repair `repairFront` (the ONLY public repair — private `repairFoldoversSweep` + `repairCoverageSweep` alternated to a JOINT fixed point until a full cycle changes nothing, because a coverage force-refine can re-fold and a foldover force-refine can re-open coverage; a sequential foldover-then-coverage order left foldovers — a shipped bug now fixed + pinned by a named regression; runs on the **jitter-free** view-proj; `jointRepairSweeps()` ≤ unrefined+1; the cone canNOT retire it — coverage is screen-space, foldover topological). The per-face geometry lives in **pure `detail::` classifiers** — `isFoldover` and `classifyCoverageRepair` (→ `CoverageRepair{kind ∈ None/AllInactiveCorners/WorstInactiveCorner}`), with the shared `detail::kMinCoverageScreenAreaPx` screen-area policy — so the sweeps apply targets a classifier returns and the parallel snapshot detector (`vdpm_parallel`) reuses the identical projection math. Finally `emitActiveIndices` (wedge-restored per-frame index buffer — the out-param overload fills a caller-owned reused vector so the per-frame path allocates nothing steady-state; `activeAncestor` is memoised per frame; `foldoversRepaired()`/`coverageRepaired()` counters feed the overlay). Headless `test_vipm.cpp` / `test_vdpm.cpp`. |
| `graphics/vdpm_parallel.hpp` + `vdpm_parallel.cpp` | **GPU-shaped CPU model of the VDPM front (Stage 0 of the GPU-driven-front arc — the algorithm de-risker, tested in CI against the recursive `vdpm` oracle BEFORE any GLSL).** `buildDependencyDag`: the refine-dependency DAG (`removingSplit[parent/vl/vr] → split`, what `forceRefine` recurses) with per-split topological **ranks**, packed into a **CSR by-rank layout** (`splitsByRank`/`rankOffsets`, the GPU uploader's shape), plus the per-split **dependency triple** (`dependencies[s]` = `SplitDependencies{parent, vl, vr}` = `removingSplit[parent/vl/vr]`, `kNoSplit` for a root/boundary, slots kept **separate** so the DAG's per-slot edge correspondence is preserved — distinct from `dependents_`, which the refine bumps from the VERTEX slots, not these dependency splits; maps 1:1 onto the GPU record) — the ONE authority the closure + the GPU B3 uploader share; `validateForest` rejects a cyclic OR structurally malformed forest (the gate before GPU upload). `ParallelFront`: the same state as `ActiveFront` but updated by **rank-ordered data-parallel passes** — requirement-closure (descending rank) → refine (ascending) → coarsen (descending), factored into `closeAndApplyRequired` (seed `required_` → close → apply, returns the refined count) — reproducing the recursive front **byte-for-byte** (proven for arbitrary scores). `repairFront`: the **snapshot** analogue of the oracle's joint repair — detect every foldover ∪ coverage violation against the **settled** front via the shared `detail::` classifiers, mark each target's removing split, `closeAndApplyRequired`, re-detect — an inflationary fixed point (≤ `initiallyUnrefined + 1` detection passes). It shares P2's per-face policy + final invariants but NOT its sequential schedule, so it may reach a **different valid front**; `repairDetectionPasses()` (GPU detect dispatches, incl. the terminal convergence pass) / `repairApplyRounds()` (= detectionPasses − 1) / `repairRefinedSplits()` are the Stage-0 overhead evidence (converges in 2 detection passes / 1 apply round, over-refines the sequential by 1–3 tris ≤0.2%). Gated on `hasMeshContext()` (scheduling-only build throws). The **full build** (from a mesh) carries the mesh context (finest faces / weld / CSR wedges); the scheduling-only build (from a bare forest) does not. `emitActiveIndices`: the GPU-shaped, seam-preserving emit — memoise active ancestors → per-face **survival flag** → exclusive **prefix sum** → stable **scatter** of three `nearestWedge`-restored corners at `out[3·slot]` (no atomic append, so original triangle order is preserved; scratch reused, output resized once), **byte-identical** to the oracle's `emitActiveIndices` for a given front (asserted after checking the two fronts are identical, so a scheduler regression can't masquerade as an emit bug). Note the **rank-local concurrency contract** and the **GPU detection contract** (same-rank splits / face-detection threads atomically share `required_`/`dependents_` — the GLSL needs atomic OR / add-sub + a barrier between dispatches) and the free `validateFrontInvariants` (reconstructs `dependents_`, checks dependency activeness + the active↔refined relation + every flag exactly 0/1 — the SHARED validator `validateInvariants` delegates to; templated over flag width with uint8/uint32 overloads so the GPU B3 harness validates 32-bit read-back state with NO lossy narrowing, precondition a valid forest; run every frame in tests). Headless `test_vdpm_parallel.cpp` (+ `[.][RankEvidence]` dispatch-depth + the wedge-ABI evidence). |
| `render/vdpm_gpu.hpp` + `vdpm_gpu.cpp` + `shaders/vdpm_score.comp` | **VDPM GPU-front scoring (Stage B1 of the GPU-driven-front arc).** Pack helpers (`packVdpmSplit` / `packVdpmPosition` / `packVdpmScoreParams`) convert the Vulkan-free authority (`makeVdpmViewParams` / `scoreVdpmSplit`, in `graphics/vdpm`) into the std430 ABI (`render/ubo.hpp` — `VdpmSplitGpu`/`VdpmScoreOut`/`VdpmScoreParams`, fully offset-asserted; `worldLinear` a padded 3-col mat3, flags uint32). `VdpmGpuMesh` (SHARED static splits + canonical positions, device-local, uploaded once) vs `VdpmGpuFront` (PER-INSTANCE score output + per-frame mapped-BDA params) keep static geometry separate from instance state — no static SSBO duplicated per instance. `recordScore` dispatches the score compute (typed buffer_reference, no descriptors; only the params block's address is pushed) and records **NO barriers** — the consumer owns sync. `shaders/vdpm_score.comp` reproduces `scoreVdpmSplit` split-by-split (camera-relative affine distance + object-space cone; back-face path writes zero to all channels). Cross-checked in the LOCAL `[.][gpu]` harness (`test_vdpm_gpu.cpp`, a headless surface-free device + compute-only `Resources` + a compute→transfer→host readback) against the CPU authority — scores close, back-face exact; the pack helpers run in normal CI (`[vdpm]`). |
| `graphics/vdpm_wedge_choices.hpp` + `vdpm_wedge_choices.cpp` | **Precomputed GPU emit acceleration (Stage B2).** `buildWedgeChoices` bakes the CPU's `nearestWedge` decision for each (original vertex, ancestor depth) into a CSR (`choices`/`offsets` by depth, `maxDepth` the walk bound), so the GPU scatter is pure integer indexing — byte-identity with the CPU emit is **structural**, not float-dependent (a GPU-recomputed distance could tie-break a near-equal wedge differently). `buildRemovalParent` collapses the removal chain to one dependent load per canonical (`splits[removingSplit[c]].parent`, or `c` at a root — an inactive self-parent fails the walk immediately). Both Vulkan-free, walks capped at `vertexCount` (throw on cycle), 64-bit offset accumulation (throw > `UINT32_MAX`); normal-CI `[vdpm]` tests. |
| `render/vdpm_scan.hpp` + `vdpm_scan.cpp` + `shaders/vdpm_scan_block.comp` + `vdpm_scan_add.comp` | **Reusable GPU exclusive scan (Stage B2).** `VdpmScan` — recursive hierarchical Blelloch scan, `kScanElementsPerBlock = 256`-element blocks (up-sweep/down-sweep per block, block sums scanned one level up, added back down). `deviceSupported`/`requireSupported` gate on the compute limits (invocations/size[0]/shared-mem ≥ 256) BEFORE pipeline creation (private `Checked` token). `hierarchy`/`scratchElementCounts` size the per-level scratch (64-bit intermediates — no `n+255` wrap near `UINT32_MAX`); `recordScan` writes the grand total to a caller buffer, empty count → no dispatch (caller pre-zeros the total). Per-level compute→compute barriers; `[vdpm]` sizing test + `[.][gpu]` stress through B²+1. |
| `render/vdpm_gpu.hpp` + `vdpm_gpu.cpp` (emit) + `shaders/vdpm_ancestor.comp` + `vdpm_survival.comp` + `vdpm_scatter.comp` + `vdpm_emit_finalize.comp` | **VDPM GPU-front emit (Stage B2) — byte-identical to `ParallelFront::emitActiveIndices`.** The FULL `VdpmGpuMesh::build` overload adds static emit data (indices/weld/removal-parent/wedge CSR; validates `indices % 3`, in-range, 32-bit count) and sets `binding().hasEmitData`; `VdpmGpuFront::buildWithEmit` allocates the single-buffered emit workspace (score-only build/front leave it absent — `recordEmit` throws). `recordEmit` uploads the front's `active` (per canonical 0/1) then records the four passes around `VdpmScan`: **ancestor** (`vdpm_ancestor.comp`, bounded removal-parent walk → active ancestor + depth, atomic failure counter, sentinel writes INVALID id + defined depth; the loop tests the vertex reached AFTER the final allowed transition so an active root exactly `maxDepth` away is accepted) → **survival** (three distinct non-failed ancestors) → scan (→ stable per-face slot + surviving total) → **scatter** (restored-wedge corners at `3·slot`, original face order, no atomic append) + **finalize** (1 invocation, index count = `3·survivors`, always dispatched). ONE 3-uint counters buffer `[failures, survivingTotal, emittedCount]` cleared once (`fillBuffer`, `eClear`→compute barrier) so all three start defined without CPU readback; compute→compute barriers between passes, one after the scan feeding BOTH consumers, none after (caller syncs). `VdpmEmitPipelines` builds the five pipelines once. LOCAL `[.][gpu]` harness proves byte-identity (indices/order/wedges) on coarsest/partial/full/repaired sphere + seamed-grid fronts + determinism + empty-mesh + all-faces-collapse (faceCount > 0) + a deepest-chain off-by-one fixture; ABI pack structs in normal CI (`[vdpm]`). |
| `render/vdpm_gpu.hpp` + `vdpm_gpu.cpp` (refine/coarsen) + `shaders/vdpm_mark.comp` + `vdpm_close.comp` + `vdpm_refine.comp` + `vdpm_coarsen.comp` | **VDPM GPU-front refine/coarsen (Stage B3) — INTEGER-EXACT to `ParallelFront::applyView`.** The BASE `VdpmGpuMesh::build` also builds the front topology from `buildDependencyDag`: `VdpmFrontSplitGpu` (32 B — vertex slots parent/child/vl/vr for `dependents`/child activation + the dependency-split triple parentDep/vlDep/vrDep for closure, 1:1 with `DependencyDag::dependencies`), `splitsByRank`, and the CPU-side `RankRange` list (copied into the front). `VdpmGpuFront::buildWithFront` allocates the persistent device-resident state (active init = roots, refined/dependents/required = 0) + the uploaded `VdpmScoreOut` input + the 2-uint failFlags. `recordApplyView` = upload scores → clear failFlags → **mark** (`vdpm_mark`, full-overwrite the refine seed `backface==0 && max(geometry,uv,normal,tangent) > budget` — straddle excluded) → `recordCloseAndRefineRequired` → **coarsen** (DESCENDING, `backface || score < coarsenBudget`, leaf-gated, atomic-sub with underflow flag). The **shared `recordCloseAndRefineRequired`** (reused verbatim by B4's repair round) does **close** (DESCENDING, atomic-OR the dependency triple) then **refine** (ASCENDING, activate child + atomic-add `dependents` per vertex-slot; a required refine with an inactive dependency sets the failure flag and does NOT mutate) and OWNS its boundary barriers — leading seed→closure, between every rank, trailing refine→consumer — so `mark→…→coarsen` and `detect→…→next-detect` are both safe. A leading `recordApplyView` barrier serialises two back-to-back cycles (the persistent-front WAW on `required`/`failFlags`). The refine/coarsen recorders drive one dispatch per rank from the CPU `RankRange` list (`(offset,count)` push constants); the SAME list is ALSO uploaded device-local (`binding.rankRangesAddress`, the `VdpmRankRangeGpu` ABI in ubo.hpp) for the coming persistent repair kernel — one authority, two consumers. `fitsComputeDispatchLimits` + `build` reject an over-large rank dispatch before allocation. LOCAL `[.][gpu]` harness (`test_vdpm_gpu_front.cpp`) cross-checks active/refined/dependents element-exact vs the CPU model + the shared `validateFrontInvariants` on read-back state, failFlags == 0, across moving-view / per-channel / diamond / deep-chain / hot-cold / back-facing / back-to-back / single-rank+empty fixtures; `[B3Evidence]` reports `1+3(maxRank+1)` dispatches/instance-frame (sphere 58, deep grid(65) 940 — the batching lever for B5). **Apply-kernel arc (checkpoint 0 showed apply owns ~86% of the post-repair-kernel commands / ~58% GPU time): the mark/coarsen policy + the `ScoreOut` struct/reduction are now the shared pure include `shaders/vdpm_apply_classify.glsl` (`combinedScore`/`applyMarkRequired`/`applyCoarsenEligible`) consumed by `vdpm_score/mark/coarsen` (one authority; no behaviour change, the B3 cross-check unchanged), and `shaders/vdpm_apply_kernel.comp` + `VdpmApplyKernel` run the WHOLE apply (reset failFlags → mark → close → refine → coarsen) for one front in ONE workgroup / ONE dispatch — the close/refine loops are the repair kernel's, coarsen ports `vdpm_coarsen.comp`. Batch-ready `VdpmApplyJobGpu` ABI (ubo.hpp; budgets IN the job, no params block); `VdpmGpuFront::prepareApplyJob` (the public pack authority, reused by the manager's batched path) + `recordApplyKernel` (write job, leading score→kernel barrier, `dispatch(1,1,1)`). **NOW SELECTED by `recordFrame` in production** (nullable `const VdpmApplyKernel* applyKernel`, no default; manager holds a `std::optional<VdpmApplyKernel>` emplaced iff `deviceSupported`; the recorder `recordApplyScoredView` is the fallback). Workgroup size is a spec constant (`ComputeSpecializationConstant` in compute_pipeline; `layout(local_size_x_id = 0)`); the `[ApplySizeSweep]` benchmark (3 fronts, GPU-timestamped, warm-up excluded, median over rotated alternating cycles) picked **256** (fastest on the curved sphere, tie on a deep grid). `analyticComputeCost` gained `persistentApply` ({1,1}); `[vdpm]` pins the four persistentApply×persistentRepair combos + empty boundaries (a selected-but-early-out kernel counts as zero). `test_vdpm_apply_kernel.cpp` (`ApplyCrossCheckRunner`, `[.][gpu]`): two `buildRuntime` fronts scored identically (PROVEN — both score buffers read back + `REQUIRE` equal), one applied by the kernel + one by the recorder, asserting active/refined/dependents/required/failFlags **BIT-EXACT** + `validateFrontInvariants`, across full-refine→coarsen / alternating-budgets / diamond deps / back-facing coarsen, plus gates for the in-kernel failFlags reset, coarsen dependents-underflow, a zero-split no-op, and a deep rank chain. Bit-exact because apply is integer state, a single deterministic pass; the recorder is separately oracle-validated, so kernel==recorder gives kernel==oracle-scheduling. **Measured (helmet): 64→~10 dispatches, apply GPU ~1.2→~0.4 ms + CPU-record ~0.17→~0.005 ms, total VdpmCompute ~2.0→~1.2–1.5 ms; 0-VUID helmet + TransmissionTest + DamagedHelmetBlend. Repair (~0.75 ms) is now the dominant GPU stage — next lever is apply+repair fusion / front-batching.** |
| `render/vdpm_gpu.hpp` + `vdpm_gpu.cpp` (repair) + `shaders/vdpm_repair_detect.comp` + `vdpm_repair_fallback.comp` + `shaders/vdpm_repair_classify.glsl` | **VDPM GPU-front repair fixpoint (Stage B4) — the P2 contract, NOT integer-exact (screen-space FP).** (Perf-arc groundwork: the foldover/coverage screen-space policy is now the PURE shared include `vdpm_repair_classify.glsl` — `classifyRepairFace(...) → {foldover, coverageKind, worstLocal}`, no buffer access / atomics / marking — so the detect shader **and** the coming persistent repair kernel share one classifier; `vdpm_repair_detect.comp` keeps the buffer reads + atomics + required-marking. Shader `#include`s resolve via `-I shaders`; a change to any `SHADER_INCLUDES` rebuilds every shader. The per-rank `RankRange{offset,count}` list is now ALSO uploaded device-local (`binding.rankRangesAddress`, asserted std430 {uint,uint}, validated at build as a contiguous partition of `splitsByRank`) for the kernel; the CPU list stays for the current recorder. **Stage 2 — the persistent-workgroup kernel** (`shaders/vdpm_repair_kernel.comp` + `VdpmRepairKernel`): ONE workgroup per front runs the WHOLE fixpoint (clear all transient state → bounded rounds of ancestor→detect→uniform-exit-if-clean→close/refine → final detect → internal full-detail fallback) in a SINGLE dispatch, replacing the recorder's ~1000 commands. Batch vehicle = the `VdpmRepairJobGpu` ABI (ubo.hpp — 17 addresses + counts; the shader reads `jobs[gl_WorkGroupID.x]`, addresses as uvec2 to avoid shader int64); push is only `{jobsAddress, jobCount}`. `s_anyMarked` shared reduction drives the workgroup-uniform convergence exit; one `wgsync()` (`memoryBarrierBuffer`+`barrier`) between phases. Capability-gated independently (`deviceSupported`). `VdpmGpuFront::prepareRepairJob` (write params ring + pack) is the public assembly authority — the raw `makeRepairJob` packer is PRIVATE, so a caller can't pack a job without first uploading its params; the manager's batched path reuses prepareRepairJob for its N-job array; `recordRepairKernel` uploads params+job, a leading apply→repair compute barrier, one `dispatch(1,1,1)`. **Stage 3 flipped it into production:** `recordFrame` takes a nullable `const VdpmRepairKernel*` and drives repair through the kernel when non-null (`VdpmGpuManager` holds a `std::optional<VdpmRepairKernel>`, emplaced iff `deviceSupported`), else through `recordRepairRuntime` — the recorder path (`recordRepairImpl`), retained as tested reference/fallback; the kernel branch adds an explicit kernel→emit compute barrier (the emit counter clear orders only the counter, not the kernel's active-front writes). `analyticComputeCost(rank, faceCount, finestFaceCount, roundBudget, persistentApply, persistentRepair)` is EXACT (not an estimate): lifecycle barrier + emit always counted (emit dispatches over the raw `faceCount`, so a zero-rank front is NOT free), the emit scan cost derived from `faceCount` (K internal levels → `{2K+1, 2K}`), and — crucially — the REPAIR term gated on the POST-WELD `finestFaceCount` (repair early-outs at `finestFaceCount == 0`, which can differ from `faceCount`; recordFrame records ONE apply→emit barrier there on BOTH the kernel and recorder paths, so repair is `{0, 1}` not `{1, 2}` — the barrier is gated on `splitCount > 0 && (kernel-repair || finestFaceCount == 0)`, so a zero-split front records none and a recorder repair *with* faces relies on its own trailing barrier). `persistentApply`/`persistentRepair` select each stage's kernel vs recorder (helmet R=18: both-kernels **{10, 10}**, recorder-apply+kernel-repair {64, 65}, both recorders {1014, 1066}). A `[vdpm]` CI test pins all four combos across scan tiers + the zero-rank + zero-repair-face boundaries. `recordFrame`'s `repairKernel` arg has **no default** — the repair path is an explicit choice at every call site (a forgotten argument must not silently pick the ~1000-command recorder). `test_vdpm_repair_kernel.cpp`: 6 `[.][gpu]` control-flow fixtures (already-clean / normal-repair / budget-0-fallback / coarsest-deepest-chain / oversized-reject / two-slot-ring) **+ 4 Stage-3 cross-checks** (`CrossCheckRunner`: two `buildRuntime` fronts settle from one mesh to a proven bit-identical pre-repair state — active/refined/dependents/required/failFlags read back + compared — then kernel-repair one, recorder-repair the other, and assert post-repair front + emitted indices + control/history + zero-fail match EXACTLY, plus a CPU-oracle gate (`validateFrontInvariants` + `foldoverCount==0` + `coverageFailures==0` + emitted≤finest) on the kernel front, across normal-convergence / forced-fallback / reflected+cull / non-uniform+no-cull; GPU-vs-GPU exact on tested fixtures, NOT a universal-visual-parity claim). Measured helmet flip: CPU record 2.1→0.26 ms, GPU compute ~17.5→~2.4 ms, 1014→64 dispatches.) The snapshot analogue of `ParallelFront::repairFront`, run after `recordApplyView`. The full `VdpmGpuMesh::build` also uploads `canonicalFaces(weld, indices)` (== `ParallelFront::finestFaces()`) + `removingSplit`; `buildWithFront` allocates the repair scratch (ancestor cache, `VdpmRepairParams` host buffer, the 4-uint `repairControl` SEPARATE from failFlags). `recordRepair` runs a bounded **round budget** of [clear `required` → **shared ancestor resolve** (`recordAncestorResolve`, factored out of `recordEmit`; live front → ancestor cache; failures → `repairControl[1]`) → **detect** (`vdpm_repair_detect.comp`: per canonical finest face, a faithful port of `detail::isFoldover` (world winding) + `detail::classifyCoverageRepair` (back-face gate → project → sub-pixel area → degenerate → centroid-inside-replacement → worst screen-displaced corner), atomic-OR each violation's inactive-corner removing split + `repairControl[0]`) → `recordCloseAndRefineRequired`], then a final detect + a **full-detail fallback** (`vdpm_repair_fallback.comp`: iff `anyMarked`, seed every unrefined split → refine to full detail, always hole-free; sets `repairControl[2]`) — so convergence is GPU-resident with NO per-round readback. Sync: a leading compute→(compute\|clear) barrier (applyView's coarsen has none) + a per-round compute→eClear→compute `required` reset. `recordDetectClassify` (test only) writes each face's packed `{foldover, coverageKind, worstLocal}` (`kVdpmDetect*`) so the harness cross-checks the GPU classifier against `detail::` per face. LOCAL `[.][gpu]` harness (`test_vdpm_gpu_repair.cpp`) proves zero CPU-classified foldovers + coverage failures (shared `test::foldoverCount`/`coverageFailures` on a read-back-front adapter) + valid invariants + emitted ≤ full detail; DIRECT per-face classification == CPU **exactly** across 6 mixed fronts (all branches); the tiny-budget fallback fires + stays hole-free. |
| `render/vdpm_gpu.hpp` + `vdpm_gpu.cpp` (runtime) + `shaders/vdpm_emit_finalize.comp` | **VDPM GPU-front combined runtime (Stage B5a) — the production unit B5b wires in.** `VdpmGpuFront::buildRuntime` combines scoring + persistent refine/coarsen state + repair scratch + emit workspace in ONE front, RINGING the draw-consumed outputs (`FrameOutput`: emitted indices / counters / indirect) + host repair params per frame-in-flight (persistent front per instance, transient output per slot — accessed by `emittedIndicesBuffer(frameIndex)` / `emittedIndirectBuffer(frameIndex)` / `countersBuffer(frameIndex)`). Two GPU data seams drop the host round-trip: **`recordApplyScoredView`** reads the front's OWN score output (the mark/coarsen `max(4 channels)` reduction, so `recordScore`'s output feeds them directly) and **`recordEmitFromFront(frameIndex)`** reads the live `activeState` — both delegate to shared `recordApplyViewImpl`/`recordEmitImpl`/`recordRepairImpl` recorders (a raw device address is never public; the host-span overloads still work for the B2/B3 isolation tests). `vdpm_emit_finalize` now writes the full 5-word draw indirect command (all fields every frame, incl. the zero-face `indexCount 0, instanceCount 1` no-op) into a buffer laid out as the SINGLE authority `graphics::DrawIndexedIndirectCommand` (already asserted bit-for-bit vs `VkDrawIndexedIndirectCommand` in resources.cpp — no VDPM-local mirror). **`recordFrame(frameIndex, scoreView, repairParams, budgets, roundBudget)`** opens with a **lifecycle-boundary barrier** (prior-compute reads+writes → this-frame compute reads+writes) — the score buffer + internal emit scratch are single-buffered, so a prior frame's mark/coarsen READS of the score output must be ordered before this frame's `recordScore` WRITE (a WAR the per-pass barriers, which sit after the score dispatch, can't cover; it's also the only sync on a zero-split mesh where score/apply/repair early-out) — then chains score → apply-scored → repair → emit GPU-only (no consumer barrier — the renderer adds the compute→index/indirect barrier). buildRuntime SHARES the repair + emit ancestor cache (repair completes before emit) to save a per-instance buffer. LOCAL `[.][gpu]` harness (`test_vdpm_gpu_runtime.cpp`) SPLITS the contract: off-threshold (cull-off + tiny budget) exact front + emit vs the full CPU lifecycle (`scoreVdpmSplit`→applyView→repairFront→emitActiveIndices); general fixtures require only a valid hole-free front + emitted ≤ finest + clean diagnostics + all 5 indirect words + `indexCount == counters[2]`; a two-frame back-to-back submit (no CPU wait) proves the ring keeps distinct slot outputs. |
| `graphics/vdpm_gpu_registry.hpp` + `vdpm_gpu_registry.cpp`, `render/vdpm_gpu_manager.hpp` + `vdpm_gpu_manager.cpp` | **VDPM GPU-front renderer integration (Stage B5b) — registration + GPU-driven draw.** The Vulkan-free **registration seam** (`vdpm_gpu_registry.hpp`): `VdpmGpuRegistry` (abstract — `available()`/`registerMesh(verts,indices,collapses)→VdpmMeshHandle`/`createFront(mesh)→VdpmFrontHandle`, all load-time), the generational identity handles `VdpmMeshHandle`/`VdpmFrontHandle` (in `gpu_handle.hpp` beside the resource handles — mesh = per-geometry shared forest, front = per-instance), and `VdpmWorkRequest` (the SEMANTIC per-instance inputs: front handle + world + material channel scales + raster cull policy; `sameParams` is field-by-field, padding-immune — the renderer dedups on it). `render::VdpmGpuManager` implements it: owns the reusable score/refine/repair/emit pipeline bundles + a `GenerationalSlotPool`-keyed per-geometry mesh table (`registerMesh`: derive forest → `fitsComputeDispatchLimits` gate, logged-once fallback → `VdpmGpuMesh::build`) and per-instance front table (`createFront`: `VdpmGpuFront::buildRuntime`), resolves live handles (stale/null → `NullBuffer`), and `recordRequests(cmd, requests, VdpmFrameGlobals)` derives each front's `VdpmViewParams`/`VdpmRepairParams` (via the SAME `makeVdpmViewParams` the CPU front calls, so params match) and drives `recordFrame`. The Renderer builds the manager ONLY past `VdpmScan::deviceSupported` (unsupported → null, CPU front usable, ctor never fails; built regardless of the runtime selector so it can toggle without a reload), exposes `vdpmRegistry()` to the load path, and per frame (when `RenderTunables::vdpmGpuBackend` / `--vdpm-gpu`) hands the sink + the visible forward front handles to the Vulkan-free, allocation-free (caller-owned `VdpmRequestSelectScratch` + output) `selectVisibleVdpmRequests` — which keeps only fronts whose FORWARD draw survived the camera cull (shadow-only ⇒ no compute) and dedups by handle (identical duplicate collapsed; **conflicting params for one front throws `std::logic_error`**, never first-wins) — then records after `collectDrawCommands`. **B5b-2 flips the draw to the GPU output:** a GPU-backed instance (backend + live front) SKIPS the CPU `refineForView`/`repairFront`/`emitActiveIndices` in `writeForwardUniforms` (the per-instance CPU-cost win); `buildDrawCommands` tags the forward `DrawCommand` (`vdpmGpuFront`) + sets `indexType UInt32`/`indexCount 0`; the Renderer resolves each tag to the GPU-emitted index+indirect ring via `VdpmGpuManager::resolveDrawBuffers` (ONE lookup, **throws on an unresolvable tag** — a zero-`indexCount` draw must never slip through; legitimate CPU fallback is a NullVdpmFront binding that never reaches here) and points `indexBuffer`/`indirectBuffer` at it (`recordIndexedDraw` unchanged); the compute→(index+indirect read) barrier is delayed to just before the depth prepass, after the shadow pass. Per-mesh fallback keeps the CPU path; stale CPU stats for GPU instances are suppressed via a per-binding `vdpmCpuRanThisFrame` flag until B5c. Verified 0-VUID drawing GPU output on the helmet (opaque+prepass) + TransmissionTest (13 fronts, transmission). Load path: `GltfLoader::loadScene` → `Geometry::load`(registers forest once) → `Object::load`(creates front). **Front-batching arc — `recordRequests` now resolves every request ONCE into a reused scratch (front* + derived params + `hasApply`/`hasRepair` work flags) then ROUTES: BOTH kernels available and not a profiled single front ⇒ the STAGE-MAJOR `recordBatched` (one lifecycle barrier → score all → one `dispatch(Na)` apply → one `dispatch(Nr)` repair over COMPACTED, chunked job arrays → one final front-state→emit barrier → emit all); else the unchanged per-front `recordFrame` loop (`recordPerFront`), which a profiled single front keeps for its per-stage GPU stamps. Two manager-owned host-visible BDA job arrays (apply/repair, one buffer per frame-slot; `ensureJobCapacity` grows geometrically, RETAINING replaced arrays for the session so an in-flight frame never reads a freed BDA; `writeMapped` subspan writes; `sizeof(job) % 8 == 0` `static_assert`ed for the shader's `buffer_reference_align`); a batch past `maxComputeWorkGroupCountX` CHUNKS by advancing the BDA `firstJob*stride` with no inter-chunk barrier. Accounting is the BATCHED aggregate `VdpmGpuFront::analyticBatchedCost(fronts, groupCap)` (NOT the per-front sum), pinned by a `[vdpm]` test. `frontForTest` is a TEST-ONLY read-only front accessor for the cross-check. `test_vdpm_gpu_manager.cpp` gained a `[.][gpu]` BIT-EXACT cross-check driving N fronts batched-via-the-manager vs per-front `recordFrame` (active/refined/dependents/required/failFlags + indirect + emitted indices equal) across distinct per-request params, compaction (full/apply-only/zero-split), shuffled order, N=1, a growth boundary, and two frame slots; state-fair `[.][gpu][BatchBench]` measured ~12× apply + ~12× repair on 13 fronts.** **B5c-1 GPU-sourced overlay diagnostics — `recordHealthReduction` (end of `recordRequests`, inside the VdpmCompute timestamp) fills a per-front `VdpmHealthJobGpu` array (counters/repairControl/roundHistory/failFlags addresses + a submitted-draw multiplier matched BY HANDLE from `VdpmFrontDrawCount`, NOT deduped requests) and dispatches the single-invocation `shaders/vdpm_health_reduce.comp` → ONE `VdpmSceneHealthGpu` in a device-local per-frame-slot ring (64-bit emitted {lo,hi} manual carry; repair-front/max+sum-marked/fallback/non-clean/ancestor/B3-fail counts — health, NOT CPU foldover/coverage). `recordDiagnosticReadback` (after the timestamp) parses the ring slot from a full cycle ago + that frame's stored `cpuTriangleSubtotal_`, then compute→transfer copy + transfer→host barrier + stores this frame's subtotal (slot order: parse-old → copy-new → store). `SceneHealth` replaces the representative-front `Diagnostics`; the renderer combines it into a frame-consistent `trianglesDrawn` and the overlay shows a separate GPU-health section (CPU rows labelled "CPU fronts only", channel attribution "n/a" for GPU; `vdpmCpuRanThisFrame` kept). `test_vdpm_health_reduce.cpp` drives the reducer directly (mixed repair / submitted-draw weighting / dirty-prefix / fallback / 64-bit carry); the manager test pins the K-frame delay + slot isolation.** **B5c-2 real-asset evidence — `test_vdpm_helmet_evidence.cpp` (`[.][gpu]`, local-only) decodes the REAL DamagedHelmet through `GltfLoader::loadScene` + `geometry.collapses()` (no re-simplify) and asserts two cleanly-separated claims: (A) from a CPU-uploaded active set the GPU emit is BYTE-IDENTICAL to `ParallelFront::emitActiveIndices` at roots/mid/full (mid asserted strictly between; the non-full cases asserted to actually reach nonzero ancestor depth + multi-wedge seam buckets among SURVIVING faces, so the match can't be vacuous); (B) the full GPU lifecycle is invariant-valid + hole-free + clean-failFlags + bounded, with the exact GPU==CPU comparison GUARDED on the reference reaching full detail (the real helmet at tiny-budget+cull-off does NOT — 8 zero-score splits remain — so the guard correctly skips, validating that "tiny budget" ≠ full refinement). Evidence WARNs report maxRank + the actual uploaded static footprint via `VdpmGpuMesh::staticByteFootprint()` (accumulated at every upload site across all twelve static B2–B4 buffers — uploaded payload bytes, no VMA suballocation padding) and its `wedgeMapByteFootprint()` subset (choices + offsets).** **B5c-4 default flip — `DebugOptions::vdpmGpuBackend` is a tri-state `std::optional<bool>` (`--vdpm-gpu` on / `--no-vdpm-gpu` off / unset), resolved in the Renderer ctor as `debug.vdpmGpuBackend.value_or(VdpmScan::deviceSupported(device_))` so the GPU front is the DEFAULT where supported; repeated flags last-one-wins (`test_application_args`). Stale B5b-1 "shadow run" comments cleared (renderer.hpp / render_tunables.hpp / frame_info.hpp / object.cpp). Completes the B5b→B5c productionization arc.** Tests: `test_vdpm_gpu_manager.cpp` (`[.][gpu]` — registration/resolve-or-throw/record + the batched cross-check + the scene-health delay/isolation) + `test_vdpm_gpu_registry.cpp` (CI — `sameParams`/handle packing/dedup) + `test_vdpm_health_reduce.cpp` (`[.][gpu]`) + `test_vdpm_helmet_evidence.cpp` (`[.][gpu]`). |
| `graphics/cloth.hpp` + `cloth.cpp` | Vulkan-free cloth data model: `ClothMesh` (particles + distance constraints + render verts + CSR normal adjacency), `makeGridCloth` and `makeClothFromMesh` (welds an arbitrary glTF mesh into particles, builds structural mesh-edge + triangle-adjacency bend constraints, resolves pin rules), the **greedy edge-colouring** (`colourConstraints`) that makes the GPU solve race-free, and `buildNormalAdjacency` (per-vertex→triangle CSR for runtime normals). Per-type compliance (structural/shear/bend). Plus `ClothCollider` + builders (plane/sphere/box/capsule). Unit-tested headless (`test_cloth.cpp`). |
| `graphics/skin.hpp` + `skin.cpp` | Joint node ptrs + inverse bind matrices; computes joint matrices from cached node worlds. |
| `graphics/lighting.hpp`, `particle.hpp`, `draw_command.hpp`, `frame_info.hpp` | Packed render-facing light record; world-space `EmitterState` (the `ParticleEmitter` gather output); backend-agnostic handle bundle; Vulkan-free per-frame data. `draw_command.hpp` also carries the **indirect-draw** contract: `DrawIndexedIndirectCommand` (a Vulkan-free 5-word mirror of `VkDrawIndexedIndirectCommand`, layout-asserted against the Vk type in `resources.cpp`) and `DrawCommand::indirectBuffer`/`indirectOffset` — a **non-null `indirectBuffer` selects `drawIndexedIndirect`** (Stage A of the GPU-driven front; only VDPM draws set it, every other path draws direct). `DrawCommand::vdpmGpuFront` (Stage B5b) tags a forward draw with its GPU-driven front so the renderer harvests the camera-visible fronts; `FrameInfo::vdpmGpuBackend` + `vdpmRequestSink` carry the runtime backend selector + the renderer-owned `VdpmWorkRequest` sink `Object` appends to. |
| `graphics/assets.hpp` | Central arrays (textures/materials/geometries/skins/animations/lights). **Backed by `std::deque`** so pointers survive insertion — important. |
| `graphics/object.hpp` + `src/graphics/object.cpp` | **High-attention (~560 lines).** Writes mapped UBOs, **selects alpha pipeline from material state** (but only returns a `PipelineHandle` — binding happens later), emits `DrawCommand`s. Also runs the **per-draw LOD** work: `selectLod` (discrete, forward + coarser for shadows) / `selectVipm` (Continuous morph uniforms) / the per-instance VDPM `refineForView` + joint `repairFront` → per-frame dynamic index buffer (ViewDependent takes precedence). Sets `cmd.indexBuffer`/`indexCount`/`lodLevel`. For VDPM it also writes a per-instance per-frame `DrawIndexedIndirectCommand` (from the emit count, every field reset) and sets `cmd.indirectBuffer`/`indirectOffset`; the **shadow copy explicitly clears** those (shadows keep discrete LOD + direct draw). Watch the per-`currentFrame` mapped-UBO/index/indirect writes. |

## Tier 7 — Collision & physics (read [`collision.md`](collision.md) alongside)

| File | Pay attention to |
|---|---|
| `collision/aabb.hpp` | Single AABB definition + `Axis` enum used by everyone. No per-TU axis helpers. |
| `collision/aabb_bvh.hpp` | Generic fat-AABB BVH (`AabbBvh<T>`, SAH insert + AVL balance, à la Box2D). `createProxy`/`moveProxy`/`destroyProxy`/`query`. **The reusable core (P6):** the physics `DynamicAabbTreeBroadPhase`, the static-mesh triangle BVH, **and** the render-side `SceneCuller` all build on it. `traverse(predicate, fn)` is the generic descent that lets a frustum query it without `collision/` depending on `graphics/`. |
| `collision/end_point.hpp`, `collider.hpp` + `collider.cpp` | Endpoints live inside `Collider`; local/world/swept AABB, layer/mask, six endpoints. `update(world, motion)` extends the swept AABB by a predicted displacement so the broadphase stays motion-aware for CCD. |
| `collision/broad_phase.hpp` | The `BroadPhase` interface (`addCollider`/`removeCollider`/`update`/`rebuild`/`possiblePairs`/`validate`) + the `CollisionPair` value type. `PhysicsWorld` owns one through a `unique_ptr` — **default `DynamicAabbTreeBroadPhase`, injectable via `PhysicsWorld(unique_ptr<BroadPhase>)`.** `validate()` cross-checks the maintained pair set against brute-force O(n²). |
| `collision/dynamic_aabb_tree_broad_phase.hpp` + `.cpp` | The default broadphase: wraps `AabbBvh<Collider*>` + a proxy-by-id map. Emits `possiblePairs_` **sorted by `(firstId, secondId)`** (independent of tree shape / map order) — the canonical order the solver relies on. |
| `collision/sweep_and_prune_broad_phase.hpp` + `.cpp` | The selectable alternative. **Sharp edge: non-owning.** Stores pointers to endpoints owned inside colliders — never move/destroy a registered collider; layer/mask change needs `rebuild()`. Emits pairs in update order; `PhysicsWorld::contacts()` re-sorts to the canonical order, so the solve matches the tree (the `Determinism.BroadphasesAgree` test asserts this). |
| `collision/world_shape.hpp`, `contact_manifold.hpp`, `geometry.hpp` + `geometry.cpp` | Neutral world-space shapes (sphere/box/capsule/**convex hull**), the `ContactManifold` value type, and reusable closest-point primitives (segment, OBB, segment-segment). Read before `narrow_phase`. |
| `collision/support.hpp`, `gjk_epa.hpp` + `gjk_epa.cpp` | **Convex query core (P3.5).** `supportPoint` (farthest point of a shape along a direction); `gjkEpaContact` = GJK distance/witnesses (separated) + EPA depth/normal (overlap) over support functions, with a directional-MTV fallback for the symmetric degeneracy. The reusable convex contact the P6 mesh + the P7 queries reuse. **Edge/corner robustness (P7.5 ✅):** on a flat face/edge the box support tie-breaks to a corner and the GJK loop stalls with a stale simplex vertex, so the closest-point *magnitude* over-estimated the gap (up to ~55%). Fixed in the outer loop, not the sub-distance: track the lower-bound separation `dot(w, v)/|v|` (the separating plane *through* the closest feature, exact at convergence), add duplicate-support termination, and report that gap + normal rather than the stale `|v|`. (Signed-volumes was tried first and abandoned — it reproduced the bug and regressed face cases, because the sub-distance was never the fault.) The `closestOnSimplex` degenerate-tetra guard still handles the *separated-large-box* false-collision. Gated by the `test_gjk_epa.cpp` fuzz suite (exact face + edge/corner). |
| `collision/ray.hpp` + `.cpp`, `shape_cast.hpp` + `.cpp` | **Query primitives (P7).** `rayIntersect` (analytic ray vs sphere/OBB/capsule/convex + Möller–Trumbore triangle + slab reject) and `shapeCast` (GJK conservative advancement, reusing `gjkEpaContact`). Tested in `test_shape_cast.cpp` (+ via `test_physics_query.cpp`). |
| `collision/narrow_phase.hpp` + `.cpp` | `collide(a, b, speculativeMargin)` = shape-specific manifold dispatch. Primitive pairs → analytic `collidePair` (+ box/box SAT); **any convex-involving pair → `collideConvex`** (GJK/EPA → polytope face-clip or single point). Normal points `b → a`. A non-zero margin emits *separated-but-approaching* gap contacts with **negative penetration** (speculative-margin CCD); margin 0 = overlap-only. |
| `physics/collider_shape.hpp`, `contact.hpp`, `physics_body.hpp` + `physics_body.cpp` | Authored shapes (incl. `ConvexHullShape` = vertices + face loops); `PhysicsWorld::worldShape` composes them with the body transform into a `WorldShape` for the narrowphase. `PhysicsBody` carries `inverseInertiaLocal` (diagonal, set from shape+mass in `createCollider`). `DebugContact` carries real manifold points. |
| `core/convex_hull_builder.hpp` + `.cpp` | `buildConvexHull(positions, indices)` welds a triangle mesh and coplanar-merges it into ordered polygon faces (a `ConvexHullShape`); `isConvex` validates. Used by the glTF `Shape: "ConvexHull"` path (`GltfLoader::meshConvexHull`). |
| `physics/contact_solver.hpp` + `.cpp`, `physics_constants.hpp` | **TGS soft-step contact solver with rotation (P9.2).** Decoupled from `PhysicsWorld` (flat `SolverBody` + `SolverContactInput`): `prepare`(at substep `h`)/`warmStart`/`solveVelocity(useBias)`/`applyRestitution`/`solvePosition`(kinematic-only)/`store`. Normal + **2D Coulomb friction** (P9.3): friction is solved as a coupled 2-vector over the contact tangent plane against a symmetric **2×2 tangent mass** (captures the tangents' angular cross-coupling) and clamped to the friction **disk** `|λ| ≤ μ·N` (a circle, not a per-axis box) — that coupling + circular clamp is what stops a tipping/edge contact pumping spurious torque. Friction has **no cross-frame memory** (only the normal impulse warm-starts across frames; replaying friction pumps energy at a rocking contact). Penetration is a **soft constraint** (`b2MakeSoft`: `biasRate`/`massScale`/`impulseScale`) with separation recomputed each substep from body-local anchors (`adjustedSeparation`). The caller runs the substep loop (warm-start → `solveVelocity(true)` bias → integrate → `solveVelocity(false)` relax); restitution is one end-of-step pass at the true impact velocity (`relVelN0`, gated below `kRestitutionThreshold`, only where `maxNormalImpulse > 0`); `solvePosition` is the leftover **kinematic-only** split-impulse pass (Dynamic `positionWeight` is 0). Speculative gaps (separation > 0) brake to `separation/h`. Persistent warm-start cache (lookup-only → determinism-safe). **Angular (P3):** effective mass `invMassA+invMassB+(rA×d)·IA⁻¹(rA×d)+(rB×d)·IB⁻¹(rB×d)`, every impulse adds `I⁻¹(r×P)` torque. Unit-tested in `test_contact_solver.cpp`. |
| `physics/physics_world.hpp` + `src/physics/physics_world*.cpp` | **High-attention.** The public API is in the header; implementation is split by responsibility. `physics_world.cpp` keeps body/collider/joint creation, fixed-step contact generation, island solving, and sleep. `physics_world_shapes.cpp` owns owner-pose/world-shape composition and collider AABB refresh/reset. `physics_world_queries.cpp` owns `raycast`/`shapecast`/`overlap*`. `physics_world_articulation.cpp` owns articulation handles, link colliders, self/static contact gather, and articulation stepping/sleep. `physics_world_debug.cpp` owns debug extraction. `physics_world_events.cpp` owns cloth-collider export and trigger/collision event bookkeeping. Study `step` (narrowphase → `solveAndIntegrate`), `contactForPair`, `solveAndIntegrate` + `solveIsland` (the TGS substep loop: prepare-once → per substep gravity/warm-start/bias-solve/integrate/relax → restitution → kinematic position pass), and the Static/Kinematic/Dynamic authority rules. `contacts()` sorts pairs into canonical `(firstId, secondId)` order so the order-dependent solve is broadphase-independent. The broadphase is injectable via `PhysicsWorld(unique_ptr<BroadPhase>)`. |
| `physics/physics_query.hpp`, `collision_event.hpp` | **P7 value types.** `QueryFilter` + `Raycast`/`Shapecast`/`OverlapHit` (carry collider+body handles); `EventPhase` + `ContactEvent` (ordered collider-handle pair + phase). |
| `physics/character_controller.hpp` + `.cpp` | **Kinematic capsule controller (P7).** `move(world, displacement)` = collide-and-slide over the queries: vertical/horizontal swept separately, blocked motion projected, slope-limit drops upward slide off too-steep faces, lift→walk→**sweep-and-rest** step-up, capsule-sweep distance + raycast-normal grounding. The step-up drop rests on the step's top edge (a downward sweep, no slide) so a rounded capsule climbs at walk speed instead of sliding off; a step is accepted only when the **lifted walk clears the obstacle** (rejects walls/steep slopes without the ambiguous edge-normal stutter). The horizontal slide ignores **walkable** ground/edge contacts (only walls block lateral travel) — without this the capsule wedges against a step's top edge descending. A `move()` output guard clamps any net-**backward** horizontal step (a rare riser depenetration against the travel) to a no-progress frame. Headless-tested (`test_character_controller.cpp`) at the demo's 0.05/frame cadence — walk-speed step-up + patrol-replay traversal/no-wedge/no-stall tests, the coverage the old 0.3/frame tests lacked. Driven from `FireEngine::mainLoop` (`-k` demo: a step-pyramid patrol; advanced at the **real per-frame dt** for smooth motion at any refresh rate, bounds-based turnaround at the flat ends, gravity only while airborne), not a scene-`Components` member — [`roadmap.md`](roadmap.md) § Design reviews explains why. |

## Tier 8 — Loading (largest translation layer)

| File | Pay attention to |
|---|---|
| `core/shader_loader.hpp` + `.cpp` | SPIR-V loading — simple, read first. |
| `core/tangent_generator.hpp` + `.cpp` | Generates tangents when base/clearcoat normal mapping needs them. |
| `core/gltf_loader.hpp` | One header declaring all `GltfLoader::` members; the `.cpp` is split by concern across `gltf_loader_*.cpp`. |
| `src/core/gltf_loader_scene.cpp` | `loadScene` entry point — drives parse → asset load → node/scene build. |
| `src/core/gltf_loader_nodes.cpp` | Node loading + hierarchy build, physics-body creation, `Controllable`/TRS application. **Note the animated-node hierarchy split** that protects authored base transforms from sampled animator transforms. |
| `src/core/gltf_loader_assets.cpp` | `parseAsset`, extension validation, asset presize, `extras` dispatch. |
| `src/core/gltf_loader_extras.cpp` | `extras.Controllable`/`Physics`/`Cloth`/`Ragdoll` parsing. |
| `src/core/gltf_loader_mesh.cpp` | Primitives/material factors/textures/samplers/UV transforms, convex-hull + triangle extraction (physics shapes), `loadMesh` assembling texture slots. **Sharp edge: base-normal and clearcoat-normal are both tangent-space — if tangents fail, skip both consistently.** |
| `src/core/gltf_loader_geometry.cpp` | **High-attention.** Geometry/vertex loading, smooth-normal + tangent generation (feeds the mesh sharp edge above). |
| `src/core/gltf_loader_material.cpp` | Material factor + texture-slot resolution. |
| `src/core/gltf_loader_images.cpp` | Centralized image source loading: direct local URI path or resolved bytes, then `Image`/`KtxImage`; texture-index lookup. |
| `src/core/gltf_loader_animation.cpp` | Animation channels + skin/inverse-bind loading. |

## Tier 9 — Vulkan resources & pipeline plumbing

| File | Pay attention to |
|---|---|
| `render/device.hpp` + `device.cpp` | Instance/surface/physical+logical device/queues, and the process-wide **VMA allocator** (`allocator()`, owned here as a `VmaAllocatorHandle`; buffers/images sub-allocate from it — `createBuffer` returns a `UniqueVmaBuffer`). Built with `VULKAN_HPP_NO_CONSTRUCTORS` — designated initializers everywhere. Device selection: the required features are one set of `kRequiredFeatures*` tables (`{pointer-to-member, name}`) that drive **both** `missingDeviceCapabilities` (the suitability check, `collectMissingFeatures`) and `createLogicalDevice`'s enable-chain (`enableFeatures`) — so check and enable can't drift. `missingDeviceCapabilities` also verifies descriptor-indexing update-after-bind limits (a separate properties check); an unsuitable GPU is rejected with a named reason. `Device::headlessCompute()` is a surface-free production mode (no GLFW/WSI extensions, no surface/swapchain, a single graphics+compute queue, present family absent) for offscreen compute — the VDPM GPU harness. |
| `render/vma.hpp` | Layer 3 of the GPU resource model (see CLAUDE.md): the VMA include shim + `VmaAllocatorHandle` and the move-only `UniqueVmaBuffer`/`UniqueVmaImage` RAII wrappers (own resource + sub-allocation together, Approach A; `operator*` yields the `vk::` handle to mirror vk::raii). Implementation TU: `render/vma_impl.cpp`. |
| `render/generational_slot_pool.hpp` + `.cpp` | Pure index+generation slot allocator behind the texture free-list (CR-17): `acquire`/`release`/`valid`, recycles released slots so the table stays bounded across resize churn. Unit-tested headless in `tests/render/test_generational_slot_pool.cpp`. |
| `render/swapchain.hpp` + `swapchain.cpp` | Images/views/extent/depth/recreation. |
| `render/frame.hpp` + `frame.cpp` | Command pool/buffers + sync objects: binary `imageAvail`/`renderDone` semaphores (WSI acquire/present), plus one monotonic **timeline semaphore** for CPU↔GPU frame pacing (replaced the in-flight fences). Renderer holds the per-slot/per-image timeline values. |
| `graphics/renderable_scene.hpp` | The Vulkan-free render↔scene seam (CR-09): the `RenderableScene` interface (`activeCamera`/`gatherLights`/`gatherEmitters`/`buildDrawCommands`) that `SceneGraph` implements and `Renderer` pulls through. The **active camera crosses here too** — `activeCamera()` returns a `CameraView{position,target}`, so `drawFrame` takes no camera arg (the scene owns which node is active; `SceneGraph::update` must run first). The renderer builds a `graphics/frame_info.hpp` `FrameInfo` per frame (retired the old `RenderContext`) and passes it plus cull frustums in; the scene emits `DrawCommand`s back. |
| `render/viewport.hpp`, `cubemap_basis.hpp` | Small render helpers. |
| `render/descriptor_bindings.hpp` | **Cross-file lynchpin.** `ForwardBinding`/`ForwardGlobalBinding`/`ShadowBinding`/`SkyboxBinding`/`PostProcessBinding` must match every `layout(set,binding)` in GLSL. |
| `render/ubo.hpp` | **High-attention.** Structs memcpy'd into mapped GPU memory — field order/`alignas`/padding must mirror GLSL std140 exactly. `test_ubo.cpp` guards sizes/offsets. The forward pass splits its data across **`ObjectUBO`** (per-object: model/hasSkin/previousModel — set 0 binding 0) and **`CameraUBO`** (per-frame: view/proj/cameraPos/view-projections — set 0 binding 29, written once/frame by the Renderer, pushed per draw so the depth prepass gets it too); both must match the blocks in `shader.vert`/`shader.frag`. Note `LightUBO::environmentParams` packs debug-view + no-shadows. |
| `render/descriptors.hpp` + `descriptors.cpp` | Forward set 0 (seven per-object/per-draw buffers: frame, camera, skin, previous-skin, morph, morph-target, VIPM) is **pushed inline per draw** via `pushForwardObjectDescriptors` (`VK_KHR_push_descriptor`) — not allocated. The shadow set 0 is **pushed inline per draw** too (`pushShadowObjectDescriptors`: per-object ShadowUBO + reused skin/morph buffers + the shared self-shadow image/sampler from `Resources`) — no per-object allocation in either pass. Set 1 (forward globals): `createGlobalDescriptors` once at startup; **`updateGlobalDescriptors` after resize** so recreated sceneColor sampler isn't dangling. Watch info-object lifetime around `updateDescriptorSets`. Set 2 (bindless materials) is owned by `Resources`, not here. |
| `render/pipeline.hpp` + `pipeline.cpp` | Forward pipelines declare `bindings` (set 0), `globalBindings` (set 1), and `bindlessSet` (set 2: a partially-bound `sampler2D[kMaxBindlessTextures]` + materials SSBO, with update-after-bind binding flags); non-forward pipelines leave globals/bindless off. Fullscreen/fragment-only config factories share private helpers; `test_pipeline_config.cpp` guards the returned state. |
| `render/resources` (bindless) | Owns the global set-2 descriptor (update-after-bind pool). `registerBindlessTexture` writes each 2D material texture into the array at its handle index (called from the 2D `createTexture` paths). `registerMaterial` assigns a material its slot in the persistently-mapped materials[] SSBO (dedup by `Material*`), returning the index drawn with. |
| `render/render_target.hpp` + `render_target.cpp` | `RenderTarget` descriptor (attachment formats for `VkPipelineRenderingCreateInfo`) + `makeRenderingInfo` helper. Dynamic rendering — no `VkRenderPass`/`VkFramebuffer`; passes transition layouts with explicit synchronization2 barriers. |
| `render/compute_pipeline.hpp` + `compute_pipeline.cpp` | Compute counterpart to `Pipeline` (single compute stage, `VkComputePipelineCreateInfo`) + synchronization2 **buffer**-barrier helpers (`makeBufferMemoryBarrier`/`recordBufferBarrier`). Used by `ParticleSystem`. |
| `render/resources.hpp` + `src/render/resources.cpp` | **The bridge. High-attention (~1300 lines).** Buffer/texture creation (`createDeviceLocalBuffer` — staged upload for static vertices/indices/LODs/VIPM; `createHostVisibleBuffer` + mapped-buffer helpers for per-frame CPU-written data — incl. `createMappedIndirectBuffers` for VDPM's indirect commands, next to the `static_assert`s pinning `DrawIndexedIndirectCommand` to `VkDrawIndexedIndirectCommand`; `uploadImageFromHost`, `createTexture2DTarget`, `withOneTimeSubmit`), descriptor pools (per-group, renderer-lifetime, **no `eFreeDescriptorSet`** — sets are plain handles owned by the pool), pipeline registry, all shadow-map resources, HDR/bloom/IBL/BRDF targets, `SharedTextures` aggregate. Buffers/images are VMA-backed (`UniqueVmaBuffer`/`UniqueVmaImage`); texture slots are managed by a `GenerationalSlotPool` so `releaseTexture`/`createTexture` recycle slots and stale handles are detectable (`validTexture`). Load-time uploads — textures **and** device-local buffers — coalesce through `begin/endUploadBatch` (one submit + fence, bracketed in `FireEngine::run`); `createDeviceLocalBuffer` records into the open batch when active, else `withOneTimeSubmit` (waits on a per-submit fence, not `queue.waitIdle`). **Mapped UBO pointers: always write the `currentFrame` slot.** A **compute-only ctor** (`Resources(const Device&)`) skips all bindless/descriptor infrastructure (no graphics `Pipeline`) — only the buffer table + upload command pool — for a headless compute device. `createDeviceLocalStorageBuffer` (device-local + BDA, optional `eTransferSrc` readback) and `createMappedReadbackBuffers` back the VDPM GPU scoring buffers. |

## Tier 10 — Frame rendering & orchestration (hardest bugs live here)

| File | Pay attention to |
|---|---|
| `render/environment_precompute.hpp` + `.cpp` | Equirect→cubemap, irradiance, prefilter, BRDF LUT at startup. |
| `render/shadows.hpp` + `shadows.cpp` | **High-attention.** CSM directional + world-only CSM, spot layers, point cubemap-array, **dual-depth per-skinned-object self-shadow** (two passes: capture nearest surface, then `cullMode=eFront` for next occluder; in-shader `skinnedSelfShadowDepthEpsilon` safety net). `kMaxSkinnedSelfShadowCasters` cap. `recordPass` takes the shadow matrices + `cullingEnabled` and filters each cascade/spot/point-face draw list against its own `Frustum` (self-shadow slots aren't culled). |
| `render/post_processing.hpp` + `post_processing.cpp` | HDR target, bloom chain, ACES/gamma. |
| `render/draw_record.hpp` | Tiny shared `recordIndexedDraw(cmd, dc, resources)` — the ONE place the indirect sentinel is honoured, so the three VDPM draw sites (forward, depth prepass, transmission) can't drift: non-null `indirectBuffer` → `drawIndexedIndirect` (explicit stride `sizeof(VkDrawIndexedIndirectCommand)`), else direct `drawIndexed`. |
| `render/transmission.hpp` + `transmission.cpp` | **High-attention.** `KHR_materials_transmission` off the captured `sceneColor`. The `shader.frag` split (post-fix): clear/frosted glass does screen-space refraction (roughness-blurred by the sceneColor mip chain); a thin-walled surface that is **also emissive** (a self-lit paper lamp shade) instead scatters to a view-independent irradiance tint — so a bright bulb behind it doesn't beam a camera-tracking blob. Discriminator is the **emissive factor**, NOT thickness. Plus back-face normal flip. Its forward recorder shares the main recorder's descriptor-order invariant: after a pipeline transition, push set 0 before binding allocated sets 1/2 through the same layout. |
| `render/debug_draw.hpp` + `debug_draw.cpp` | Physics debug wireframes. `PhysicsDebugData` (AABBs + `ClothCollider` shapes + `DebugContact`s + free-form `DebugLine`s `queryLines` (query probe + the ragdoll joint RGB gizmo) + `DebugLabel`s `jointLabels` (drawn as projected ImGui text by the overlay, not line geometry), all Vulkan-free) → CPU-built line list in a per-frame mapped `Vertex` buffer (`Resources::createMappedVertexBuffers`) → line-list pipeline (`Pipeline::debugLineConfig`, `PipelineConfig::topology` + `dynamicDepthTest`) drawn into HDR after particles. Brackets the HDR target ShaderReadOnly↔ColorAttachment + depth ReadOnly→Attachment. Physics side: `PhysicsWorld::debugColliderBounds()` / `debugContacts()` (captured in `step()` pre-resolve). |
| `tests/support/vdpm.hpp` | Shared VDPM front validators — `foldoverCount` / `coverageFailures`, templated on any front exposing `forest()` + `active(v)`, so the sequential `ActiveFront` and parallel `ParallelFront` repairs are judged by the **same first-principles yardstick** (recomputed from the mesh, never the repair under test). Coverage keeps its geometry independent but shares the one runtime tuning knob (`detail::kMinCoverageScreenAreaPx`, viewport-derived) so the "worth-fixing" threshold tracks the repair's; checks only **projectable** coverage (near-plane straddles go to the separate path). |
| `tests/physics/test_physics_determinism.cpp` + `tests/support/state_hash.hpp` | Determinism harness: FNV-1a body-state hash; ReplayIsBitIdentical / FreeFallMatchesClosedForm / GoldenHash / **BroadphasesAgree** (same scene through the tree + an injected SAP → identical hash). The GoldenHash constant is a behaviour tripwire — update it intentionally when the solver math changes. |
| `render/ssao.hpp` + `ssao.cpp` | **High-attention.** SSAO + contact shadows. Runs after the depth prepass: borrows the shared scene depth (attachment → read-only → attachment), reconstructs view position+normal from depth alone (analytic unprojection from `proj`, no normal G-buffer), writes R8G8 (R = hemisphere-kernel AO, G = sun-direction contact-shadow ray-march; `ssao.frag`), then a **depth-aware bilateral blur** (`ssao_blur.frag`, view-space-Z edge-stop) into a second target. Always runs (disabled = intensity 0). Forward set 1 binding 13 samples the blurred target. The depth prepass itself is `Pipeline::depthPrepassConfig` + `Renderer::recordDepthPrepass` (reuses `shader.vert` w/ `invariant gl_Position`; forward loads depth `LESS_OR_EQUAL`). |
| `render/taa.hpp` + `taa.cpp` | **High-attention.** Temporal AA subsystem. Owns the RG16F velocity target (written by the forward/transmission passes as a 2nd colour attachment), two ping-pong history HDR targets, and the resolve pass (`taa.frag`): reproject history along velocity → 3×3 neighbourhood clamp → blend → blit into the offscreen HDR target. `historyWritten_` guards the first frame after (re)create. Sub-pixel jitter lives in `Renderer::drawFrame`; motion vectors are jitter-free. |
| `render/particle_system.hpp` + `particle_system.cpp` | Renderer-owned GPU particle system. Pooled SSBO partitioned per emitter; compute sim (`particle_simulate.comp`) → buffer barrier → instanced additive billboards into HDR (soft particles via sampled scene depth). Records after the TAA resolve (un-jittered, kept out of history), before post-process. |
| `render/soft_body_system.hpp` + `soft_body_system.cpp` | **High-attention.** GPU XPBD cloth solver. Descriptor-free: four compute pipelines (`cloth_predict`/`solve`/`collide`/`finalize`) take every buffer as a `bufferDeviceAddress` pointer in the push constant — per-cloth particle/constraint buffers + the render vertex buffer + a per-frame collider buffer, all `eShaderDeviceAddress`. `recordSolve` = per-substep `predict → per-colour solve → collide`, then `finalize` writes solved positions + normals (recomputed from the per-cloth CSR adjacency, arbitrary topology) into the cloth's storage vertex buffer (compute-write → vertex-input-read barrier). Reads `ClothSimParams` (overlay; compliance is a global multiplier on each constraint's authored per-type stiffness); colliders from `PhysicsWorld::gatherColliders`. Cloths come from the `-c` demo or glTF `extras.Cloth`. |
| `render/render_tunables.hpp` | Plain struct of live, overlay-editable render params (TAA, **`cullingEnabled`**, **`lodEnabled`/`lodPixelErrorBudget`**, debug view, bloom/IBL/sun, particle scales) + the `DebugView` enum (incl. `Lod` tint and `Joints` — the latter has no shader branch: it suppresses the scene mesh and draws the ragdoll joint gizmo/labels instead, and maps to `None` for the shader). Seeded from `constants.hpp` + CLI flags; the renderer reads it instead of the `constexpr`s. Read this first — it's the contract between the overlay and the renderer. |
| `render/gpu_profiler.hpp` + `gpu_profiler.cpp` | Timestamp `VkQueryPool` ring (`kMaxFramesInFlight` slots). `begin/end(pass)` write a pair; `resolve` reads the slot a cycle later (safe — the acquire timeline-wait guarantees that frame finished) into `FrameStats`. `slotUsed_` guards reading never-reset queries; `eWithAvailability` skips passes that didn't run. Disabled when `timestampPeriod==0` / `timestampValidBits==0`. `FrameStats` also carries the frustum-cull tracked/culled counts (populated in `collectDrawCommands`, shown a frame later) and `vdpmGpuAvailable` (B5c-3 — set there from `vdpmManager_ != nullptr`, i.e. device capability independent of whether the GPU front is currently active; drives the overlay's backend checkbox enable/label). |
| `render/debug_overlay.hpp` + `debug_overlay.cpp` | Dear ImGui owner (context + GLFW/Vulkan backends, dynamic rendering). `buildUi(stats, tunables)` builds the panels (incl. the **Culling** group: `cullingEnabled` toggle + tracked/visible/culled readout); `record` draws into the swap image (loadOp Load). `drawWorldLabels(labels, viewProj)` projects world-anchored `DebugLabel`s (ragdoll joint index:name) into the ImGui **foreground draw list** — call it after buildUi and after the frame's `viewProj` is finalised; it maps via `DisplaySize` (retina-correct, not the pixel extent). The Mesh LOD panel's view-dependent block carries the **B5c-3 "GPU-driven front" backend checkbox** (writes `tunables.vdpmGpuBackend`, a reload-free flip — see the manager construction invariant); gated on `stats.vdpmGpuAvailable`, else a disabled checkbox + explicit "(unsupported on this device)" label rather than a silent disable. Non-movable (ImGui global state). ImGui core plus GLFW/Vulkan backends come from the vcpkg `imgui[glfw-binding,vulkan-binding]` manifest dependency; `cmake/fireengine_imgui.cmake` wraps the ImGui archive so `fireengine` keeps direct ownership of Vulkan/GLFW linkage. |
| `render/renderer.hpp` + `src/render/renderer.cpp` | **Capstone.** `drawFrame` = named phases (`updateFrameLighting`/`collectDrawCommands`/`recordShadowPass`/`recordForwardPass`/`recordTransmissionPass`/`recordPostProcessing`) plus the inline `taa_.recordResolve`, `recordParticlePass`, `overlay_.record`, and `transitionSwapchainToPresent` (present-split: post-process leaves the swap image in colour-attachment layout). Each pass is wrapped in a `profiler_` scope. Reads `tunables_` for debug view, IBL/bloom/sun, TAA params, particle scales. Study pass ordering, the jitter-free `currentViewProj_`/`previousViewProj_` matrices, `recreateSwapchain` + `buildGlobalDescriptorRequest`. `collectDrawCommands` runs the coarse frustum pre-cull (builds camera + shadow frustums → `scene.cull`); `buildDrawBuckets` does the precise per-camera cull. **Forward command-order invariant:** when a forward pipeline becomes active, push set 0 before binding allocated sets 1/2 through that same layout; higher-set binds preserve set 0. Keep this in sync with transmission recording. |

## Tier 11 — Shaders (verify against the C++ they mirror)

Read paired with `ubo.hpp`, `descriptor_bindings.hpp`, `gpu_limits.hpp`.

| File | Pay attention to |
|---|---|
| `shader.vert` / `shader.frag` | Main forward PBR. `shader.vert` also emits jitter-free current/previous clip positions for TAA motion vectors — skinned meshes reproject through **last frame's joints** (`PrevSkin` UBO, set-0 binding 30) for exact per-vertex deformation velocity; rigid nodes use `previousModel` (unified via `prevTransform`). **`shader.frag` is the densest shader**: writes `outVelocity` (location 1) before any early return, then IBL, CSM/spot/point shadow sampling, double-sided normal flip (`if (!gl_FrontFacing) N = -N;` for `N`, `shadowNormal`, clearcoat `N_cc`), debug-view branches (incl. velocity = 5, SSAO = 6, **LOD tint = 7** — colours by `pc.lodLevel`), transmission. Array sizes must equal `gpu_limits.hpp`. |
| `taa.frag` | TAA resolve: `historyUV = uv − velocity`, 3×3 neighbourhood clamp, `mix(current, history, historyBlend)`; falls back to current when history is invalid or the reprojected UV is off-screen. Fullscreen triangle (`postprocess.vert`). |
| `shadow.vert`/`.frag`, `self_shadow_second.frag` | Skinning+morph still run here so animated geo casts matching shadows; second pass = back-faces only. |
| `skybox.*`, `postprocess.*` | Cubemap sample; ACES/gamma. |
| `environment_convert`, `irradiance_convolution`, `prefilter_environment`, `brdf_integration`, `bloom_downsample`/`upsample` | IBL precompute + bloom kernels. |
| `particle_simulate.comp`, `particle.vert`/`.frag` | GPU particle sim (spawn-claim atomics + integrate) and instanced billboards (`gl_InstanceIndex` reads the pool SSBO; soft-fade vs sampled scene depth). UBO layout mirrors `ParticleFrameUBO`/`ParticleEmitterGpu`. |
| `cloth_predict`/`solve`/`collide`/`finalize.comp` | XPBD cloth substep chain: integrate gravity+wind → per-colour distance projection (race-free, `compliance = c.compliance * scale`) → push out of colliders → write positions + adjacency-gathered normals into the `Vertex` buffer (flat-float writes, `sizeof(Vertex)==100` asserted C++-side). All buffers arrive via `GL_EXT_buffer_reference`; the shared 112-byte `Push` block (vec4s first for 16-byte alignment) mirrors `ClothPush`. |

## Tier 12 — Top of the tree (everything comes together)

| File | Pay attention to |
|---|---|
| `fire_engine.hpp` + `src/fire_engine.cpp` | **Read last.** Owns Window/Renderer/Input/SceneGraph/Assets/PhysicsWorld/Camera*. The **fixed-timestep accumulator inside a variable-rate render loop** (`mainLoop`/`stepSimulation`), which also derives `alpha = accumulator / fixedDt` and threads it into `applyPhysics`/`syncNodes` for CR-20 render interpolation, plus default-sun seeding, `-f` floor plane, and category-gated ragdoll diagnostics (`FE_LOG=ragdoll:debug`). This is your whole-frame mental model. |

---

## How to use this

- **Stop at Tier 6** if you only need to add a material field or scene feature.
- **Tiers 9–10 are the deep end** — `resources.cpp`, `renderer.cpp`, `shadows.cpp`, `shader.frag`.
  Budget the most time there.
- **Read the cross-file invariants** ([`onboarding.md`](onboarding.md) § Cross-File Invariants) before touching
  anything in Tiers 6, 9, or 11 — the compiler won't catch a slot-order or std140 mismatch.
</content>
</invoke>
